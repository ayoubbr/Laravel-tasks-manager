{{-- @extends('home')

@section('content')
    <div class="taskbox">
        <div class="content">
            <h3>
                {{ $task['title'] }}
            </h3>
            <a href="{{ route('task.images', $task->id) }}">imgs</a>
        </div>
        <div>
            @yield('images')
        </div>
    </div>
@endsection --}}
